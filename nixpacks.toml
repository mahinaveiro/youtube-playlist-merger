[phases.setup]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip", "deno", "git"]

[phases.build]
cmds = [
  "pip install --no-cache-dir -r requirements.txt"
]

[phases.deploy]
cmds = [
  "uvicorn main:app --host 0.0.0.0 --port $PORT --log-level info"
]
